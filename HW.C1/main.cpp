#include <iostream>
#include <stdlib.h>

int main(void)
{


	return 0;
}

int MaxValueRecursive(int rodSize, int* values)
{
	if (rodSize == 0)
		return 0;
	auto maxValue = INT_MIN;
	for (auto i = 1; i <= rodSize/2; ++i)
	{
		//auto currentValue = 
	}
}